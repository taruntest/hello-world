# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://help.github.com/actions/language-and-framework-guides/building-and-testing-java-with-maven

name: Java CI with tarun-01-00001

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-01:

    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v3
    - name: echo
      run: echo Hello, World
    - name: echo
      run: sleep 450

  build-02:

    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v3
    - name: echo
      run: echo Hello, World0002
    - name: echo
      run: sleep 450
      
  build-03:

    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v3
    - name: echo
      run: echo Hello, World0002
    - name: echo
      run: sleep 450
      
  build-04:

    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v3
    - name: echo
      run: echo Hello, World0002
    - name: echo
      run: sleep 450
